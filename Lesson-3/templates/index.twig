<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Document</title>
    <style>
        .container {
            width: 100%;
            height: 100vh;
            text-align: center;
            background-color: rgb(32, 26, 26);
        }

        h1 {
            font-size: 24px;
            padding-top: 30px;
            color: white;
        }

        #carousel {
            align-items: center;
            margin-left: 20px;
        }

        img {
            height: 250px;
        }
    </style>
</head>

<body>

<div class="container">
    <h1>Третьяковка v.3</h1>
    <div id="app">

        <div id="carousel">

            {% for image in images %}
                <a href="photo.php?image={{ path }}{{ image }}"><img src="{{ path }}{{ image }}" alt="{{ image }}"/></a>
            {% endfor %}
        </div>


    </div>
</div>

</body>

</html>